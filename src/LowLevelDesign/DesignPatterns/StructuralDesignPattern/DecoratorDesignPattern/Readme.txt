Structural Design pattern is used where we want to add a 
thing to a base class, and then again keping new class as 
base and addition features

The need for decorator pattern

solving issue : class Explosion (creation of many classes for each stuffs)
how it solves: it has one base class and adds features upon that base class;
The object has the base class and it is also a base class
basically it extends and holds the object; 